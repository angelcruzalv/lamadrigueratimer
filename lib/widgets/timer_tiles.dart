import 'package:flutter/material.dart';

class TimerTiles extends StatelessWidget {
  const TimerTiles({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ListView();
  }
}
